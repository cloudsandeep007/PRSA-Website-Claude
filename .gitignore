node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Legacy SQLite DB — replaced by Supabase Postgres; never re-commit a DB file (it holds real data/credentials)
server/database.sqlite
server/database.sqlite-shm
server/database.sqlite-wal

# Media now lives in Supabase Storage (see server/migrate-media.js); local uploads are no longer served
public/uploads

# One-time design scaffolding from the initial project export — not used by the app
stitch_apex_skate_academy.zip
stitch_extracted
downloaded_media

.vercel
.env*
